# This file specifies files that are *not* uploaded to Google Cloud
# using gcloud. It follows the same syntax as .gitignore
# NOTE: Este .gcloudignore es para deployments del FRONTEND desde raíz
# El backend tiene su propio .gcloudignore en backend/

.gcloudignore
.git
.gitignore

# Node.js dependencies
node_modules/

# Build output (se genera durante el build)
dist/
build/

# Environment files (se pasan como build args)
.env
.env.local

# IDE
.vscode/
.idea/

# Tests
coverage/
**/*.test.ts
**/*.spec.ts
**/*.test.tsx
**/*.spec.tsx

# NOTA: NO excluir backend/ aquí porque:
# - Este .gcloudignore se usa cuando haces submit desde la raíz
# - Si excluyes backend/, los builds del backend fallan
# - Cada componente (frontend/backend) tiene su propio .gcloudignore
# backend/
